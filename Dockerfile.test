FROM python:3.13-slim

WORKDIR /app

# Install system dependencies if required for any python packages
RUN apt-get update && apt-get install -y --no-install-recommends \
    gcc \
    && rm -rf /var/lib/apt/lists/*

# Copy the project files
COPY pyproject.toml README.md requirements.txt .env.example ./
COPY src/ ./src/
COPY tests/ ./tests/

# Install the application and all test dependencies
RUN pip install --no-cache-dir --upgrade pip && \
    pip install --no-cache-dir .[test]

# The default command is to run pytest
CMD ["pytest", "tests/", "-v"]
